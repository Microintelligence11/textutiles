{
    "builds": [{
        "src": "utilesApp/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "utilesApp/wsgi.py"
        }
    ]
}